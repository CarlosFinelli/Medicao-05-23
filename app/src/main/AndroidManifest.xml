<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaManLogistica"
        tools:targetApi="31">
        <activity
            android:name=".views.saida.SaidaActivity"
            android:exported="false" />
        <activity
            android:name=".views.pdf.PdfActivity"
            android:exported="false" />
        <activity
            android:name=".views.calendario.CalendarioActivity"
            android:exported="false" />
        <activity
            android:name=".views.chegada.ChegadaActivity"
            android:exported="false" />
        <activity
            android:name=".views.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".views.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>